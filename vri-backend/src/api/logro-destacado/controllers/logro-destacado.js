"use strict";

/**
 * logro-destacado controller
 */

const { createCoreController } = require("@strapi/strapi").factories;

module.exports = createCoreController("api::logro-destacado.logro-destacado");

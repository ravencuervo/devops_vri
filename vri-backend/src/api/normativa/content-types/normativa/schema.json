{
    "kind": "collectionType",
    "collectionName": "normativas",
    "info": {
        "singularName": "normativa",
        "pluralName": "normativas",
        "displayName": "Normativa",
        "description": "Directivas y reglamentos oficiales"
    },
    "options": {
        "draftAndPublish": true
    },
    "attributes": {
        "titulo": {
            "type": "string",
            "required": true
        },
        "descripcion": {
            "type": "text"
        },
        "tipo": {
            "type": "enumeration",
            "enum": [
                "directiva",
                "reglamento"
            ],
            "required": true
        },
        "archivo_pdf": {
            "type": "media",
            "multiple": false,
            "allowedTypes": [
                "files"
            ],
            "required": true
        }
    }
}